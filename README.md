# XXX-schoolwork
学习以及提交作业

## ex01 Homework
### Task 1: Basic HelloWorld
A simple C program that prints "Hello World".

### Task 2: Keyboard Input
Added input function to read user's name and print a personalized greeting.
2026年 3月25日 早上八点打卡
### Task lab02: 
通过定义引脚常量控制两路 LED（引脚 2、17）同步闪烁，实现串口打印状态信息，掌握引脚输出模式配置、数字电平写入与延时函数的基础用法。
### Task lab03: 
通过配置 PWM 通道（频率 5000Hz、8 位分辨率）驱动引脚 2 的 LED 实现渐变呼吸效果，掌握 ledcSetup/ledcAttachPin/ledcWrite 的 PWM 核心用法，实现 LED 亮度从 0 到 255 平滑渐变。
### Task ex02:  
ex02 实验使用 millis () 函数代替 delay ()，实现 LED 以 1 赫兹的频率稳定闪烁，避免阻塞程序运行
### Task ex03:  
ex03 实验使用 millis () 函数实现 LED 灯的 SOS 求救信号闪烁，通过精准控制亮灭时长，让 LED 按照三短、三长、三短的摩尔斯电码规律闪烁，同时避免程序阻塞
### Task ex04:  
实现触摸传感器控制 LED 自锁效果，通过边缘检测 + 软件防抖，实现 “摸一下亮、再摸一下灭”，避免触摸误触发。
### Task ex05:  
结合 PWM 呼吸灯与触摸逻辑，实现 3 档循环调速呼吸灯，触摸引脚切换档位，不同档位呼吸节奏差异显著。
### Task ex06:  
基于双通道 PWM 实现警车双闪灯效，两 LED 亮度反相渐变，通过步长 / 峰值优化，交闪效果明显且过渡柔和。
###Task ex07:
将 GitHub 仓库 pull 到本地；
创建作业 7 工作目录（ex07）；
完成 Web 网页端无极调光器 参考 PWM 呼吸灯和 Web 服务器 提示：要求修改 HTML 代码，在网页中加入一个滑动条（<input type="range" min="0" max="255">）。通过 JavaScript 监听滑动条的变动，并通过 fetch() 或 GET 请求将滑动条的具体数值发送给 ESP32。ESP32 解析 URL 中的数值并赋值给 PWM 通道。 预期实验结果：手机或电脑浏览器打开 ESP32 网页，页面上出现一个滑动条。拖动滑动条时，开发板上的 LED 亮度会随着滑动条的位置实时、平滑地改变。
###Task ex08:
创建作业 8 工作目录（ex08）；
完成物联网安防报警器模拟实验 参考基础 IO、触摸引脚、Web 服务器 提示：将 ESP32 作为一个 “安防主机”。网页端提供两个按钮：“布防 (Arm)” 和 “撤防 (Disarm)”。使用全局变量记录系统状态。 在布防状态下，如果触碰引脚，则 LED 开始狂闪（报警状态），即使手松开，LED 闪烁也不会停止，直到用户在网页端点击 “撤防”。 预期实验结果： 未布防时触摸引脚无反应；在网页点击布防后，一旦有人触碰引脚，LED 开始高频闪烁锁定报警；只有通过网页点击撤防，LED 才会熄灭并重置状态。
###Task ex09:
创建作业 9 工作目录（ex09）；
完成实时传感器 Web 仪表盘 提示：不要只做控制（下发），也要做数据采集（上报）。 要求在 ESP32 的 Web Server 中不仅下发 HTML 页面，还要能实时返回触摸传感器的模拟量数值。网页端使用 AJAX 技术不断拉取该数值，并显示在网页中央。 预期实验结果： 网页上显示着一个实时跳动的数字。当手逐渐靠近 ESP32 引脚时，网页上的数字实时变小；手指离开时，数值恢复。实现类似仪器仪表的数据实时监控面板。
